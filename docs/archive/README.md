# Archived notes

Point-in-time notes from May–June 2026, kept for history. **They do not describe
the current system** — most predate the Supabase migration, the
admin-panel-as-single-source rework, and the plant-scoped dashboard, so their
instructions are now wrong rather than merely dated.

For current state see `STATUS.md` at the repo root.

Two documents that these notes refer to are still live at the root and were
deliberately not archived: `DEPLOYMENT.md` and `SUPABASE_MIGRATION_GUIDE.md`
(both referenced from source comments).
